MARKETRADING - FINAL FIX
=========================

I preserved your existing pages/design and fixed:
- login
- registration (including the stray "javascript" line)
- Supabase client setup
- admin user loading
- admin access check
- secure admin_credit RPC call
- admin credit history without relying on a fragile FK relationship
- clearer admin errors

BEFORE TESTING
--------------
1. Open supabase.js and confirm the Supabase URL/public key are correct.
2. In Supabase SQL Editor, run SUPABASE-ADMIN-SETUP.sql.
3. Set your admin profile:
   UPDATE public.profiles
   SET role = 'admin'
   WHERE email = 'YOUR-ADMIN-EMAIL';
4. Do not put a service_role/secret key in the website.
5. Test through Live Server or a real host, not file://.

EXPECTED FLOW
-------------
Register user
 -> Auth user created
 -> profile created (or created on first login if email confirmation is enabled)
 -> login
 -> normal user goes to dashboard.html

Admin login
 -> admin profile checked
 -> admin-dashboard.html
 -> ALL profiles load
 -> admin selects a normal user
 -> admin enters amount
 -> admin_credit RPC securely checks admin
 -> profile.balance increases
 -> transaction with type admin_credit is recorded

If users still show "Unable to load users" after running the SQL,
the error text printed in the table is the exact Supabase error to use
for the next fix.
